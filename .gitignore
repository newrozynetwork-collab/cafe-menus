# Database (contains real data — do not commit to public repos)
database/tirana.db
database/*.db

# Uploaded files — seed images are committed, future admin uploads are not
uploads/*
!uploads/.gitkeep
!uploads/Slemani/

# PHP session/cache
*.session
*.cache

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
error_log
